{
  "Main": {
    "uid": {
      "type": "UID",
      "config": {
        "label": "uid"
      }
    },
    "title": {
      "type": "StructuredText",
      "config": {
        "single": "heading3, heading4, heading5, heading6",
        "label": "title"
      }
    },
    "narrative": {
      "type": "StructuredText",
      "config": {
        "multi": "preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
        "label": "narrative"
      }
    },
    "description": {
      "type": "StructuredText",
      "config": {
        "multi": "preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
        "label": "description"
      }
    },
    "hashtags": {
      "type": "StructuredText",
      "config": {
        "multi": "preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
        "label": "hashtags"
      }
    },
    "completed_on": {
      "type": "Date",
      "config": {
        "label": "completed_on"
      }
    },
    "status": {
      "type": "Select",
      "config": {
        "options": ["Completed", "Backlog", "In Development"],
        "label": "status"
      }
    },
    "image": {
      "type": "Image",
      "config": {
        "constraint": {},
        "thumbnails": [],
        "label": "image"
      }
    },
    "github": {
      "type": "Link",
      "config": {
        "allowTargetBlank": true,
        "label": "github"
      }
    },
    "url": {
      "type": "Link",
      "config": {
        "allowTargetBlank": true,
        "label": "url"
      }
    },
    "isfeatured": {
      "type": "Boolean",
      "config": {
        "default_value": false,
        "label": "isFeatured"
      }
    },
    "tech_stack": {
      "type": "Group",
      "config": {
        "fields": {
          "icon": {
            "type": "StructuredText",
            "config": {
              "single": "heading1, heading2, heading3, heading4, heading5, heading6",
              "label": "icon"
            }
          },
          "technology": {
            "type": "StructuredText",
            "config": {
              "multi": "preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
              "label": "technology"
            }
          }
        },
        "label": "tech_stack"
      }
    }
  }
}
